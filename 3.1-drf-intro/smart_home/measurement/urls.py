from django.urls import path, include
from measurement.views import SensorView
#router = routers.DefaultRouter()
#router.register('Sensor', SensorView.OrderViewSet, basename='Sensor')


urlpatterns = [
]
